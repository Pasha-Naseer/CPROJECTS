// We include the header file to import functions like printf and scanf used for data input and output
// No space between # and include + No space between <> and filename 
#include <stdio.h>

// main part of the program, There is no code that does not contain this function
int main()
{
    printf("Hello World");
    // tells OS that the code is executed successfully
    // it returns 0
    // because of that the main function is an int
    return 0;
    
}
